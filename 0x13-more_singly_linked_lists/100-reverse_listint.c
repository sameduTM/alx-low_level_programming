#include "lists.h"

/**
  * reverse_listint_t - reverses list
  * @head: pointer to first element of list
  *
  * Return: pointer to first node of reversed list
  */

listint_t *reverse_listint(listint_t **head)
{
	head++;

	return (0);
}
